import 'package:flutter/material.dart';

double? deviceHeight;
double? deviceWidth;
const double? marginPadding = 20;

const appThemeColor = Colors.red;

ThemeData trainngAppTheme = ThemeData().copyWith(
    primaryColor: Colors.red,
    appBarTheme: AppBarTheme(
        elevation: 0, centerTitle: false, backgroundColor: Colors.red));
